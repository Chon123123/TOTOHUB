
local Luna = loadstring(game:HttpGet("https://raw.githubusercontent.com/Chon123123/Uilib/refs/heads/main/source.lua.txt", true))()

local Window = Luna:CreateWindow({
    Name = "TOTO HUB | VIETNAM PIECE",
    Subtitle = nil,
    LogoID = "96284854109683",
    LoadingEnabled = false,
    LoadingTitle = "TOTO HUB",
    LoadingSubtitle = "by PopCat",

    ConfigSettings = {
        RootFolder = nil,
        ConfigFolder = "BIG Hub"
    },

    KeySystem = false,
    KeySettings = {
        Title = "Luna Example Key",
        Subtitle = "Key System",
        Note = "Best Key System Ever!",
        SaveInRoot = false,
        SaveKey = true,
        Key = {"Example Key"},
        SecondAction = {
            Enabled = true,
            Type = "Link",
            Parameter = ""
        }
    }
})

local Tab = Window:CreateTab({
    Name = "Farming",
    Icon = "shopping_cart",
    ImageSource = "Material"
})

local Button = Tab:CreateButton({
	Name = "Button Example!",
	Description = nil, -- Creates A Description For Users to know what the button does (looks bad if you use it all the time),
    	Callback = function()
         -- The function that takes place when the button is pressed
    	end
})

